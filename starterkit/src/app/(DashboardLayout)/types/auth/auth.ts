export interface registerType {
  title?: string;
  subtitle?: React.ReactElement | React.ReactElement[];
  subtext?: React.ReactElement | React.ReactElement[];
}

export interface loginType {
  title?: string;
  subtitle?: React.ReactElement | React.ReactElement[];
  subtext?: React.ReactElement | React.ReactElement[];
}

export interface signInType {
  title?: string;
}
